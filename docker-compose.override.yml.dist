# =============================================================================
# badi-menu — Dev overrides (auto-loaded by Docker Compose)
# Copy to docker-compose.override.yml for local development.
# DO NOT push to the server.
# =============================================================================

services:
  db:
    ports:
      - "${DB_PORT:-3306}:3306"
