# =============================================================================
# badi-menu — Dev overrides (auto-loaded by Docker Compose)
# DO NOT push this to the server.
# =============================================================================

services:
  db:
    ports:
      - "${DB_PORT:-3306}:3306"